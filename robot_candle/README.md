# robot_candle（简要说明）

使用 Candle 进行本地模型推理（如 Qwen3）的示例，包含基础配置与工具函数。

## 位置
- 入口代码： [src/main.rs](src/main.rs)
- 配置文件： [config.json](config.json)
- 工程配置： [Cargo.toml](Cargo.toml)

## 运行
```bash
cargo run
```

## 提示
- 若需调整模型路径/参数，请修改 `config.json`。  
- 相关实现位于 `src/` 下的模块文件。

